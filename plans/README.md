# Implementation Plans — Auritus (ariasSTT)

Generated by the `improve` skill on 2026-07-01, against commit `10b48d1`
(v0.3.3). Each executor: read the plan fully before starting, honor its STOP
conditions, and update your row when done. Plans are self-contained — you do not
need the audit or any prior conversation.

These five are the "Recommended bundle" the maintainer selected from a larger
vetted audit (see "Findings considered but not planned" below for the rest).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Verification baseline (pytest + ruff + CI) | P1 | M | — | DONE¹ |
| 002 | Stop logging transcript text by default | P1 | S | 001 | DONE² |
| 003 | Fix Linux hotkey/mode rebind busy-loop + dead Hold mode | P1 | S | 001 | DONE³ |
| 004 | Verify GGUF model integrity (truncation + optional sha256) | P2 | S | 001 | DONE⁴ |
| 005 | Verify installer integrity before silent exec | P1 | M | 001 | DONE⁵ |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Execution log — all 5 executed, reviewed, and merged to `main` on 2026-07-01

All plans were run by a dispatched executor in an isolated git worktree, reviewed
tech-lead style (done criteria re-run, scope + diff + tests audited), and
fast-forward merged to `main`. Final state: `main` at `32b17bb`, **22/22 tests
pass, ruff clean**, both `ci.yml` (push/PR) and `release.yml` (tag-only, trigger
unchanged) present.

| Plan | Commit | Branch (merged) |
|------|--------|-----------------|
| 001 | `e4a205c` test: add pytest + ruff baseline and CI job | `advisor/001-verification-baseline` |
| 002 | `6771e07` fix(privacy): don't log transcript text by default | `advisor/002-redact-transcript-log` |
| 003 | `ae783a5` fix(linux): stop orphaning the socket-IPC thread on rebind; hide dead Hold mode | `advisor/003-linux-hotkey-rebind` |
| 004 | `a1a3258` fix(gpu): verify GGUF download length + optional sha256 before use | `advisor/004-model-integrity` |
| 005 | `32b17bb` fix(update): verify installer sha256 (release sidecar) before silent exec | `advisor/005-installer-integrity` |

**Deferred follow-ups (surfaced during execution, out of the plans' scope):**
- **Tidy dead code + drop the ruff ignores.** 001's blanket `per-file-ignores`
  suppresses pre-existing unused `io`/`queue` imports in `dictate.py` and an unused
  `creationflags = 0` local in `backends/whisper_cpp_backend.py`. (005 already
  removed the `shutil` import it orphaned.) A small PR should delete these three
  and drop the ignores so ruff catches future dead code.
- **Pin the real GGUF SHA256 digests** in `MODEL_SHA256` (004 shipped them as
  `None` scaffolds — truncation guard is active; digest pinning activates once
  filled from the official source).
- **Code signing** for the installer remains the real authenticity fix beyond
  005's sidecar hash (roadmap-deferred).

**Recommended order (as executed):** 001 first (it unblocks the rest), then 002 → 003 → 004 →
005. 002/003 are the cheapest, highest-user-value fixes; 005 is the largest
(touches the release pipeline).

## Dependency notes

- **001 is a hard prerequisite for 002–005.** Each of those adds a test to the
  `tests/` harness and expects `pytest`/`ruff`/CI to exist. If 001 hasn't landed,
  002–005 each contain a fallback note, but the clean path is 001 first.
- 004 is the most independent (its tests live entirely in `tests/test_backends.py`
  and need no display). If you want a quick early win after 001, 004 is safe to
  do out of order.
- 005 edits `.github/workflows/release.yml`; 001 deliberately does NOT (it adds a
  separate `ci.yml`). No conflict, but land 001's `ci.yml` before 005 so both
  workflows exist.

## Verification commands (all plans)

- No-display floor: `python3 -m pytest tests/test_backends.py -q`
- Full suite (Linux): `xvfb-run -a python3 -m pytest -q`
- Full suite (real display / macOS / Windows): `python3 -m pytest -q`
- Lint: `python3 -m ruff check .`

The repo has **no** pre-existing test/lint tooling; plan 001 establishes it. CI
today is release-only (`.github/workflows/release.yml`, tag-triggered) and must
stay that way — plan 001 adds a new `ci.yml` for push/PR.

## Prior-audit reconciliation

A prior Codex audit exists at `docs/reviews/2026-05-14-codex-audit.md` (against
v0.3.2, before the Linux port). Verified against live v0.3.3 code:

- **Still open, in this bundle:** M4 (installer signature) → plan 005; H5 (model
  integrity) → plan 004.
- **Still open, NOT in this bundle** (see below): H1 (GPU port race), H3 (quit vs
  load race / drainer-list lock), H4 (Recorder double-open), H2 (drainer pipe
  close).
- **New since that audit** (Linux port): the socket-rebind busy-loop and dead
  Hold mode (plan 003), and the transcript-logging privacy leak (plan 002).

## Findings considered but not planned

Vetted and confirmed real, but not in the selected bundle. Recorded so they
aren't re-audited from scratch — pick any up later with another `improve` pass.

**Correctness / security (open):**
- **BUG-02 / prior H3** — `quit()` during a model swap can leak an orphaned
  `whisper-server` subprocess; `_drain_threads` list has no lock. (MED, M)
- **BUG-03 / prior H1** — GPU probe port released before `whisper-server` binds,
  no retry. (MED, S)
- **BUG-05 / prior H4+H2** — `Recorder.start()` overwrites an open stream;
  `unload()` joins drainers without explicit pipe close. Defensive. (LOW, S)
- **SEC-04** — Linux IPC socket has no explicit `chmod`; authz rests on
  `$XDG_RUNTIME_DIR` being 0700. Defense-in-depth. (MED/low, S)

**Tech-debt / deps / docs:**
- **TECH-01** — `dictate.py` god module (2833 lines, 16 `IS_WINDOWS` branches);
  do the roadmap's planned platform-shim extraction. (HIGH conf, L) — do after 001.
- **TECH-02** — overlay ↔ layer-shell indicator have no dispatch seam (6
  duplicated call-site pairs). (HIGH conf, M)
- **DEP-01 / DX-03** — `requirements.txt` fully unpinned, feeds CI directly. (S)
- **DX-02 / DOCS-02 / DEP-02** — Linux install broken: `requirements-linux.txt`
  referenced by CLAUDE.md doesn't exist; PyGObject undeclared. (S)
- **DEP-03** — `setup.bat` duplicates the dep list independent of `requirements.txt`. (S)
- **DOCS-01** — `AGENTS.md` describes the pre-Linux app (wrong architecture for
  Codex). (S) — highest-value doc fix.
- **DOCS-03/04** — `ROADMAP.md` has no Linux row; stale "~1900 LOC" (actual 2833). (S)
- **DOCS-05** — README config table omits `SHOW_OVERLAY` / `OVERLAY_POSITION` /
  `OVERLAY_MARGIN`. (S)

**Direction (maintainer's call, not defects):**
- **DIR-01** — mic-device selection runtime-editable + persisted (top pick; first
  slice of the committed first-run wizard). (S)
- **DIR-03** — plumb overlay tokens/volume/max-record into config *before*
  building the v0.3.0 Settings GUI (the GUI's hidden prerequisite). (M)
- **DIR-02** — Linux waveform-overlay parity vs the Windows glass overlay, or a
  deliberate "dot-only on Linux" decision. (M, spike)
- **DIR-05** — literal voice commands ("new line", "comma") via the existing
  `clean_text()` seam; keep local, don't bundle with LLM formatting. (S)
- **DIR-04** — meeting-recording mode as the differentiator; run the design spike
  to resolve its 4 open questions before sequencing. (L, spike)

## Findings considered and rejected

- **Performance (PERF-01)** — nothing worth doing. Single-user desktop app; the
  80 ms paste sleep is load-bearing + Windows-only, overlay redraw is a standard
  30fps loop, model reload is off the hot path. Recorded so it isn't re-audited.
